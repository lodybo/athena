const colors = require('tailwindcss/colors');

module.exports = {
  purge: [
    'src/**/*.tsx'
  ],
  darkMode: false,
  theme: {
    colors: {
      primary: colors.teal
    },
  },
  variants: {},
  plugins: [],
}
