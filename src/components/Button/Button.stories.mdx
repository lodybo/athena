import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { Button } from './Button';
import { ButtonTemplate, argTypes } from './Button.stories';

<Meta
  title="Atoms/Button"
  component={ Button }
  argTypes={{
    argTypes
  }}
  args={{
    primary: false,
  }}
/>

# Button
A simple button. Especially useful for user-initiated actions.

<Canvas>
  <Story
    name="Simple button"
    argTypes={{}}
  >
    { ButtonTemplate.bind({}) }
  </Story>
</Canvas>

If necessary, a button can also be themed in the primary colors.

<Canvas>
  <Story
    name="Primary button"
    args={{
      primary: true,
    }}
  >
    { ButtonTemplate.bind({}) }
  </Story>
</Canvas>

But, if needed, you can pass other colors to it in the form of a [classnames](https://www.npmjs.com/package/classnames) argument.

<Canvas>
  <Story
    name="Specifying a class"
    args={{
      classes: 'example-class'
    }}
  >
    { ButtonTemplate.bind({}) }
  </Story>
</Canvas>

<ArgsTable of={ Button } />