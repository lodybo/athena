import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { {{ pascalCase name }} } from './{{pascaleCase name}}';

export const {{ pascalCase name }}Template = (props) => <{{ pascaleCase name }} {...props} />;

<Meta
  title="Elements/{{ pascalCase name }}"
  component={ {{ pascalCase name }} }
  argTypes={{

  }}
/>

# {{ pascalCase name }}

<Canvas>
  <Story
    name="Simpe {{ pascalCase name }}"
    args={{

    }}
  >
    { {{ pascalCase name }}Template.bind({}) }
  </Story>
</Canvas>

<ArgsTable of={ {{ pascalCase name }} } />